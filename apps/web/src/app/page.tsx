export default function HomePage() {
  return (
    <main className="foundation-page">
      <section className="foundation-card" aria-labelledby="mercure-title">
        <p className="eyebrow">Security platform foundation</p>
        <h1 id="mercure-title">Mercure</h1>
        <p>
          The frontend composition root is online. Product modules are composed from Nx libraries and
          server-side business logic belongs to the NestJS API.
        </p>
      </section>
    </main>
  );
}
