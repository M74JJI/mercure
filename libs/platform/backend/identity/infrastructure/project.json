{
  "$schema": "../../../../../node_modules/nx/schemas/project-schema.json",
  "name": "platform-backend-identity-infrastructure",
  "projectType": "library",
  "sourceRoot": "libs/platform/backend/identity/infrastructure/src",
  "tags": ["scope:platform", "side:backend", "type:infrastructure"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p libs/platform/backend/identity/infrastructure/tsconfig.lib.json"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config libs/platform/backend/identity/infrastructure/vitest.config.ts"
      }
    }
  }
}
