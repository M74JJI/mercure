services:
  postgres:
    image: postgres:18.6-bookworm
    environment:
      POSTGRES_DB: mercure
      POSTGRES_USER: mercure
      POSTGRES_PASSWORD: mercure-local
    ports:
      - '5432:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U mercure -d mercure']
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - mercure-postgres-data:/var/lib/postgresql

volumes:
  mercure-postgres-data:
